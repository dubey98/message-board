extends layout

block content
  h1= title

  a(href="/new")
    button(type="button") Create new Message
  each message in messages
    h2= message.text
    h4= message.user
    h5= message.formatted_date()
    hr
